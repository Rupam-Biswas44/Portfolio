"use client";

/**
 * NeuralNetworkCanvas — now a lightweight CSS-only placeholder.
 * The real canvas is GlobalCanvas, mounted once in layout.tsx.
 * This component is kept for backwards compatibility but renders nothing.
 */
export default function NeuralNetworkCanvas() {
    return null;
}
